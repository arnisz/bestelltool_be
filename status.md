# Project Status: Resource Planning System (Go Backend)

## 🎯 Current Focus
**Phase 3 ist sicherheitstechnisch nachgeschärft: Die kritischen Review-Blocker in Auth-/Session-Logik (Race-Conditions, Actor-Rolle im Audit, Access-Token-TTL) sind umgesetzt und gegen echte PostgreSQL-Integration inkl. Race-Lauf verifiziert.** Nächster Schritt: Phase 4 (Admin-Endpunkte).

## ⚙️ Server-Konfiguration (Umgebungsvariablen)

### Bestehend

| Variable | Required | Default | Beschreibung |
|----------|----------|---------|-------------|
| `DATABASE_URL` | ✅ | — | PostgreSQL Connection String |
| `APP_ENV` | ✅ | — | `dev` / `staging` / `prod`; steuert die Zulässigkeit von `AUTH_STATIC_TOKENS` |
| `AUTH_MODE` | ❌ | `session` | `session` für datenbankgestützte Sitzungen oder `static` ausschließlich bei `APP_ENV=dev` als Übergang |
| `AUTH_STATIC_TOKENS` | nur `dev` | — | Statische Bearer-Token (`token:user-id:role,...`) — Übergangslösung, ab Phase 2 nur noch bei `APP_ENV=dev` zulässig (SEC-26) |
| `ENCRYPTION_KEY` | ✅ | — | Base64-kodierter AES-256-GCM-Schlüssel (exakt 32 Byte nach Dekodierung) für den verschlüsselten D-2-Successor-Refresh-Token |
| `TRUST_PROXY_HEADERS` | ❌ | `false` | Erlaubt die IP-Ermittlung aus `X-Forwarded-For`/`X-Real-IP` ausschließlich hinter einem vertrauenswürdigen Reverse Proxy |
| `RUN_MIGRATIONS` | ❌ | `false` | Führt beim Serverstart eingebettete Up-Migrationen aus (`true`/`1`) |
| `HTTP_ADDR` | ❌ | `:8080` | HTTP Listen-Adresse |
| `HTTP_READ_TIMEOUT` | ❌ | `15s` | Read-Timeout |
| `HTTP_WRITE_TIMEOUT` | ❌ | `15s` | Write-Timeout |
| `HTTP_IDLE_TIMEOUT` | ❌ | `60s` | Idle-Timeout |
| `HTTP_SHUTDOWN_TIMEOUT` | ❌ | `10s` | Graceful-Shutdown-Timeout |

### Geplant (Benutzerverwaltung, Phase 2–3)

| Variable | Required | Default | Beschreibung |
|----------|----------|---------|-------------|
| `ACCESS_TOKEN_TTL` | ❌ | `15m` | Lebensdauer des Access-Tokens |
| `REFRESH_TOKEN_TTL` | ❌ | `720h` | Lebensdauer des Refresh-Tokens (30 d, muss die Offline-Phase der Techniker abdecken) |
| `SESSION_IDLE_TTL` | ❌ | `720h` | Idle-Timeout der Session |
| `SESSION_MAX_LIFETIME` | ❌ | `2160h` | Absolute Obergrenze (90 d) |
| `REFRESH_REPLAY_GRACE` | ❌ | `30s` | Toleranzfenster für wiederholte Refresh-Requests (Entscheidung D-2); `0` deaktiviert |
| `PRINCIPAL_CACHE_TTL` | ❌ | `30s` | Obergrenze der Widerrufsverzögerung (SEC-11) |
| `ARGON2_MEMORY_KIB` | ❌ | `19456` | Argon2id-Speicher (OWASP-Minimum 19 MiB) |
| `ARGON2_TIME` | ❌ | `2` | Argon2id-Iterationen |
| `ARGON2_PARALLELISM` | ❌ | `1` | Argon2id-Parallelität |
| `LOGIN_MAX_ATTEMPTS` | ❌ | `10` | Fehlversuche bis zur zeitlichen Sperre |
| `LOGIN_LOCKOUT_WINDOW` | ❌ | `15m` | Dauer der zeitlichen Sperre |

## ✅ Completed
- [x] System Architecture and Requirements defined (`systemdesign.md`).
- [x] AI Agent rules and architectural constraints defined (`agents.md`).
- [x] Project Status tracking initialized (`status.md`).
- [x] Go-Modul initialisiert.
- [x] Hexagonale Verzeichnisstruktur erstellt.
- [x] Domain-Entitäten implementiert.
- [x] Domain-Zustandsautomaten implementiert.
- [x] Domain-Unit-Tests implementiert.
- [x] Application Ports definiert.
- [x] Unit-of-Work-Vertrag definiert.
- [x] Repository-Verträge für erste transaktionale Use Cases definiert.
- [x] AuditWriter- und IdempotencyStore-Ports definiert.
- [x] Erste transaktionale Use Cases implementiert.
- [x] Application-Unit-Tests mit In-Memory-Fakes implementiert.
- [x] PostgreSQL-Schema entworfen.
- [x] Initiale Up- und Down-Migrationen erstellt.
- [x] Audit- und Idempotency-Tabellen erstellt.
- [x] Fachliche Constraints und Indizes definiert.
- [x] Migrationen dokumentiert (`migrations/README.md`).
- [x] PostgreSQL-Connection-Pool-Adapter implementiert.
- [x] PostgreSQL-Unit-of-Work implementiert.
- [x] Transaktionsgebundene Repositories implementiert.
- [x] PostgreSQL-AuditWriter implementiert.
- [x] PostgreSQL-IdempotencyStore implementiert.
- [x] PostgreSQL-Integrationstests ergänzt (mit Skip bei fehlender `TEST_DATABASE_URL`).
- [x] Konfliktbehandlung in PostgreSQL-Repositories geschärft: reine Versionssprünge ohne fachliche Feldänderung werden als Optimistic-Locking-Konflikt behandelt (Request/Resource/Allocation).
- [x] Server-Konfiguration über Umgebungsvariablen implementiert (`DATABASE_URL`, `HTTP_ADDR`, Read/Write/Idle/Shutdown-Timeouts).
- [x] Composition Root im Server-Bootstrap verdrahtet (pgx-Pool, UnitOfWork, Repositories, Use Cases, HTTP-Server, Graceful Shutdown).
- [x] HTTP-Adapter mit Go 1.22 `ServeMux`-Method-Pattern-Routing implementiert.
- [x] Einheitliches JSON-Fehlerformat implementiert: `{"error":{"code":"...","message":"..."}}` inkl. zentralem Error-Mapping (`404/409/422/500`).
- [x] Erste Endpunkte angebunden: Request anlegen, Request abrufen, Allocation-Return anfordern (schreibend über Use Cases/UoW inkl. AuditEvents).
- [x] Handler-Tests mit `httptest` und In-Memory-Fakes ergänzt (inkl. Fehler-Mapping 404/409/422 und ungültiges JSON 400).
- [x] Authentifizierungs-Port implementiert: `ports.Principal`, `ports.Authenticator`, `ports.ErrUnauthenticated` (→ 401).
- [x] Auth-Middleware implementiert: Bearer-Token-Extraktion, Principal in Request-Context; fehlender/ungültiger Token → 401.
- [x] Security-Befund behoben: `actor_id`/`actor_role` aus `auditPayload` entfernt; `parseAuditMeta`/`firstNonEmpty` gelöscht; `buildAuditMeta` liest Actor-Identität ausschließlich aus dem Principal. Fehlender Principal → 500 (Programmierfehler). `X-Client-Occurred-At` bleibt erhalten.
- [x] StaticTokenAuthenticator implementiert (`internal/adapters/auth`), konfigurierbar über `AUTH_STATIC_TOKENS`.
- [x] Composition Root verdrahtet: Authenticator in `main.go`; `AUTH_STATIC_TOKENS` required, Startup-Fehler bei fehlendem Wert.
- [x] Handler-Tests und Auth-Tests vollständig aktualisiert und erweitert (Middleware-Tests, Actor-Feld-Negativ-Tests, Principal→AuditMeta-Test, 500-Programmierfehlertest).
- [x] PostgreSQL-Integrationstests gegen die Raspberry-Pi-Testdatenbank vollständig ausgeführt — alle 8 Tests grün. Zwei Testfehler behoben: (1) `txB` nach Context-Timeout nicht wiederverwendet (poisoned connection → `txC`); (2) `alloc-2` als `completed` (terminal) inseriert, damit die Unique-Constraint-Prüfung korrekt über `repo.Save` läuft.
- [x] Direct-Transfer-Regel in `systemdesign.md` dokumentiert (Abschnitt 3, korrekte Position nach Roles); Architecture nach Abschnitt 4 verschoben.
- [x] Domain-Zustandsautomat um Direct-Transfer-Pfad erweitert: `Resource.TransferDirect` (`in_use` → `reserved`, Block-Guard, leere HolderID-Guard) und `Allocation.CompleteDirectTransfer` (`with_technician` → `completed`, toleriert pending `ReturnRequestedAt`).
- [x] `AllocationRepository.Create` Port + PostgreSQL-Adapter implementiert (benötigt für neue Allocations innerhalb einer Transaktion).
- [x] `TransferResourceUseCase` implementiert: atomare Transaktion mit korrekter Reihenfolge (Lock: Request → Allocation → Resource; Save: alte Allocation first, dann Resource, dann neue Allocation erstellen). Auditiert beide Allocations. Override-Note bei pending Return-Request.
- [x] `ErrAlreadyCompleted` → HTTP 409 (war 422): `mapHTTPError` in `handler.go` korrigiert.
- [x] `newAuditEvent` generiert automatisch eindeutige IDs via `crypto/rand` (behebt latentes PK-Konflikt-Problem bei mehreren Audit-Events pro Transaktion).
- [x] Domain-Unit-Tests für `TransferDirect` und `CompleteDirectTransfer` ergänzt (Success, Blocked, EmptyHolder, ShippedBack, WrongState, AlreadyCompleted, PendingReturnRequest).
- [x] Use-Case-Unit-Tests für `TransferResourceUseCase` ergänzt (Success, BlockedGuard, AuditRollback, TerminalTarget, SameRequestGuard).
- [x] PostgreSQL-Integrationstests für Direct-Transfer ergänzt — alle 10 Tests grün: `TestTransferResourceWithPostgres` (voller Use Case inkl. Unique-Index-Validierung) und `TestTransferResourceConflictNewAllocWhileOldActive` (Gegentest: Index feuert korrekt).
- [x] **Rollenbasierte Autorisierung implementiert**: `ports.ErrForbidden` (→ 403), `requireRoles`-Middleware, explizite Rollenfreigabe an jeder Route. Neue Rollenkonstante `domain.ActorRoleAdmin` eingeführt. `StaticTokenAuthenticator` akzeptiert jetzt `technician`, `dispatcher`, `admin`, `system`. Berechtigungsmatrix in `systemdesign.md` dokumentiert. Architekturregel in `agents.md` festgeschrieben. Alle bestehenden Tests aktualisiert; neue Middleware- und Autorisierungsmatrix-Tests ergänzt. PostgreSQL-Integrationstests — alle 10 grün gegen Raspberry-Pi-Testdatenbank.
- [x] **Rollenbezeichnung vereinheitlicht**: Migration `000005` setzt `'dispatcher'` als einzigen kanonischen Rollenwert in `users.role` und `audit_events.actor_role` durch. `mapActorRole`-Mapping-Funktion entfernt. Alle Schichten (Domain, HTTP, Token-Config, DB) verwenden jetzt denselben Wert `"dispatcher"`. Cross-Technician-Verhalten als bewusste Designentscheidung in `systemdesign.md` dokumentiert (vollständig auditsicher durch AuditEvent pro Transaktion).
- [x] HTTP-Endpunkt für Direct Transfer angebunden (`POST /api/v1/resources/{id}/transfer`) mit strikter Rollenfreigabe nur für Dispatcher; Handler nutzt `decodeJSONBody`, `buildAuditMeta` und zentrales Fehler-Mapping. Composition Root in `main.go` verdrahtet. Handler-Tests für Success/400/401/403/404/409/422 ergänzt.
- [x] SSE-Adapter vorbereitet: neuer Event-Port (`internal/application/ports/event.go`), in-memory SSE-Broker (`internal/adapters/sse`), geschützter Stream-Endpunkt `GET /api/v1/events` mit Rollenfilter (Dispatcher: alle Events, Techniker: nur eigene). Schreibende Use Cases publizieren typisierte Events; Composition Root verdrahtet Publisher + Stream. Unit-Tests für Broker/Handler ergänzt.
- [x] SSE-Handshake gehärtet: `GET /api/v1/events` flush't jetzt direkt nach dem Schreiben der SSE-Header (vor `Subscribe`), damit Clients die Verbindung sofort erkennen, auch wenn das Stream-Backend beim Subscriben kurz blockiert. Regressionstest `TestHandleEventsFlushesBeforeSubscribeReturns` ergänzt.
- [x] Fachentscheidung finalisiert und dokumentiert: Direct Transfers und operative Ressourcenallokationen liegen ausschließlich beim Dispatcher; Techniker-zu-Techniker-Transfer ist ausgeschlossen (`systemdesign.md`).
- [x] End-to-End-Test ergänzt: `TestResourceLifecycleE2E` verifiziert den vollständigen HTTP→UseCase→Repository→PostgreSQL-Durchstich über `httptest.Server` inkl. Rollen-Negativfall `403` für Techniker-Direct-Transfer (`internal/adapters/http/e2e_test.go`, Skip ohne `TEST_DATABASE_URL`); die Vorbedingungen für Schritt C werden dabei aktuell per Roh-SQL-Fixture gesetzt (nicht über den Produktivpfad).
- [x] Migrations-Runner für den Serverstart implementiert: SQL-Migrationen via `go:embed` ins Binary eingebettet; zentraler Runner im Postgres-Adapter; optionaler Startup-Lauf über `RUN_MIGRATIONS` mit fail-fast bei Fehler.
- [x] Deployment-Strategie für automatische Migrationen beim Serverstart dokumentiert (`docs/deployment.md`): klare Umgebungsregeln für `RUN_MIGRATIONS`, Multi-Instanz-Empfehlung (`RUN_MIGRATIONS=false` in Staging/Prod), dedizierter Pre-Deployment-Migrationsschritt und manuelle Rollback-Policy für Down-Migrationen.
- [x] **Architektur um Identity-, Autorisierungs- und Benutzerverwaltungsmodell erweitert** (`systemdesign.md`, Abschnitte 5–13): Datenmodell (`users`, `auth_identities`, `roles`, `permissions`, `role_permissions`, `user_roles`, `sessions`, `refresh_tokens`), Berechtigungsmodell mit Deny-by-Default, Session-/Token-Konzept mit Rotation und Replay-Erkennung, `active_role` bei Mehrfachrollen, Audit-Erweiterung, Datenschutz-/Aufbewahrungsvorgaben, numerierte Sicherheitsanforderungen `SEC-01`–`SEC-27`, offene Entscheidungen `D-1`–`D-5` und Migrationsphasen.
- [x] **Phase 0 Deployment-Nachweise erbracht**: `compose.yaml` validiert, Backend-Container gestartet, `/healthz` liefert `200`, Migrationen beim Containerstart nachgewiesen (`RUN_MIGRATIONS=true`) und Container läuft als `65532:65532`; Image-Inspektion ohne eingebettete `AUTH_STATIC_TOKENS`.
- [x] **SEC-26 im Container negativ nachgewiesen**: `APP_ENV=prod` mit gesetztem `AUTH_STATIC_TOKENS` führt zu fatalem Startup-Abbruch.
- [x] **B5-Nachweis gegen leere Testdatenbank erbracht**: `emptydb.bat` + `go test -count=1 -p 1 ./...` mit gesetzter `TEST_DATABASE_URL` grün; Migrationsanwendung erfolgt in `testPool`/`e2eTestPool` via `RunEmbeddedMigrations` (kein `TestMain` nötig).
- [x] **`README.md` gegen den tatsächlichen Code verifiziert und korrigiert** (2026-07-26): Punkt-für-Punkt-Abgleich von „Project Status" (Differenzierung fertig/transitional/Port-ohne-Endpunkt/fehlend), `cmd/server` (kein Platzhalter mehr — vollständiger Composition Root), Go-Version (1.22 → 1.26 an allen Stellen), Architektur-Verzeichnisbaum (`internal/adapters/auth`, `internal/adapters/sse`, `docs/`, `scripts/` ergänzt), Zustandsdiagrammen (Direct-Transfer-Pfad und `externally_procured` ergänzt), „Running the Project" (Docker-Compose-Workflow inkl. `persistent.bat`/`emptydb.bat`, `/healthz`-Nachweis), Testanleitung (`TEST_DATABASE_URL`, `-p 1`-Begründung), Technology Stack (Caddy entfernt — im Repo nicht vorhanden, nur als Annahme in `TRUST_PROXY_HEADERS` erwähnt) und Lizenzabschnitt (zum Zeitpunkt dieser Verifikation keine `LICENSE`-Datei vorhanden — seither mit unverändertem Apache-2.0-Volltext nachgetragen, siehe folgender Eintrag). Alle in der neuen README genannten Befehle wurden real ausgeführt (Docker-Compose-Start, `/healthz`, Integrationstests gegen `db-test`). Kein Widerspruch zwischen `status.md` und Code gefunden.
- [x] **Vier Nacharbeiten aus der README-Verifikation umgesetzt** (2026-07-26): (1) `systemdesign.md` §3 um „External Procurement" und „Allocation Cancellation" ergänzt, beide Regeln strikt aus `internal/domain/resource.go`/`allocation.go` abgeleitet und als code-derived statt fachlich entschieden gekennzeichnet; offene Fachfragen dazu als `D-6`/`D-7` in §12 eingetragen (nicht geraten). (2) README-Deployment-Abschnitt um SEC-27-Hinweis ergänzt (Backend darf in Produktion nicht direkt exponiert werden, TLS-Terminierung durch Reverse Proxy, im lokalen Compose-Setup bewusst nicht enthalten). (3) Tech-Debt-Eintrag zu `IdempotencyStore` ergänzt (Port+Adapter ohne Aufrufer, Grep-Nachweis nur in `usecases_test.go`) und `agents.md` §5 um Klammerzusatz erweitert, dass die Prüfpflicht erst mit dem Sync-Batch-Endpunkt greift. (4) `LICENSE` mit unverändertem Apache-2.0-Volltext angelegt (Copyright-Zeile: `2026 Arnisz`, aus der Git-Konfiguration übernommen — Rechtsträger-Name vom Nutzer zu bestätigen), README-Lizenzabschnitt entsprechend aktualisiert. `go build`/`go vet`/`go test -count=1 ./...` weiterhin grün.
- [x] **Phase 1 (Audit-Fundament) vollständig umgesetzt und verifiziert** (2026-07-26): Migration `000006` (`audit_events.actor_role` um `admin`, `entity_type` um `user`/`role`/`user_role`/`resource_class`/`resource_class_membership`/`session`/`auth_identity` erweitert; Append-Only-Trigger idempotent auf `BEFORE UPDATE OR DELETE FOR EACH ROW` + `BEFORE TRUNCATE FOR EACH STATEMENT` mit `RAISE EXCEPTION ... USING ERRCODE = '42501'` gehärtet; vorbereitendes `REVOKE UPDATE, DELETE ON audit_events FROM PUBLIC`, wirksam erst mit der Rollentrennung in Phase 5). Dafür nötiges Test-Cleanup-Refactoring umgesetzt: `internal/adapters/postgres/postgres_test.go` und `internal/adapters/http/e2e_test.go` nutzen jetzt einen Schema-Reset (`DROP SCHEMA public CASCADE; CREATE SCHEMA public;` auf einer separaten `pgx.Connect`-Verbindung, strikt vor Erzeugung des `pgxpool.Pool`, gefolgt von `RunEmbeddedMigrations`) statt `TRUNCATE ... CASCADE`, da Letzteres über `audit_events.actor_id → users(id)` den neuen Append-Only-Trigger ausgelöst hätte. Aktions-Taxonomie als Konstanten festgeschrieben (`internal/domain/audit_action.go`). **Manuell gegen die echte Testdatenbank verifiziert**: zwei aufeinanderfolgende Läufe von `go test -count=1 -p 1 ./...` gegen denselben ephemeren `db-test`-Container (tmpfs, Port 5433) — keine OID- oder Caching-Fehler durch den pgx-v5-Connection-Pool nach dem Schema-Reset, der Workflow funktionierte reibungslos. Damit gilt Phase 1 als vollständig abgeschlossen; keine Admin-Endpunkte werden weiterhin vor Phase 4 ausgeliefert.
- [x] **Zwei Tech-Debt-Punkte vor Phase 2, Punkt 5 behoben** (2026-07-26): (1) **Startup-Konfigurationsbeleg (SEC-06)**: `cmd/server/main.go` schreibt jetzt kurz vor `srv.ListenAndServe()` einen strukturierten `slog.Info("server starting", "app_env", ..., "http_addr", ..., "run_migrations", ...)`-Eintrag. `AUTH_STATIC_TOKENS` und `DATABASE_URL` werden nicht geloggt (Maskierung durch Weglassen, nicht durch nachträgliches Schwärzen) — manuell gegen `db-test` verifiziert: Log zeigt `app_env=dev http_addr=... run_migrations=false`, ein testweise gesetztes `AUTH_STATIC_TOKENS`-Secret erscheint nachweislich nicht im Log (`grep`-Nachweis: 0 Treffer). (2) **SQL-Fixture-Drift im Seeding behoben**: neue Domain-Entität `domain.User` (`internal/domain/user.go`, spiegelt exakt die heutige `users`-Tabelle: `id`, `role`, `display_name`, `is_active`, `created_at`, `updated_at` — keine Vorwegnahme von `username`/`email`/`version` aus Migration `000007`); neue Ports `UserRepository`, `ResourceClassRepository` sowie `Create` auf `ResourceRepository` (vorher nur `Save`), alle in `ports.Transaction` verdrahtet (`internal/adapters/postgres/user_repository.go`, `resource_class_repository.go`, Erweiterung von `resource_repository.go`, `transaction.go`). `seedDirectTransferPreconditions` (`internal/adapters/http/e2e_test.go`) legt Request/Resource/Allocation jetzt ausschließlich über `postgres.NewUnitOfWork` + Repositories + Domain-Zustandsautomaten an (kein rohes SQL mehr) — Resource durchläuft real `Reserve→MarkIssued→MarkInUse`, Allocation `MarkShipped→MarkReceivedByTechnician`, exakt wie im echten Betrieb. `scripts/dev-seed.sql` gelöscht und durch `cmd/seed/main.go` ersetzt: eigenständiges, `APP_ENV=dev`-only Tool, das Dummy-User (technician/dispatcher/admin), eine Resource-Class und zwei Resources ausschließlich über UoW+Repositories anlegt; jeder Datensatz läuft in einer eigenen Transaktion, ein `ErrConflict` (bereits vorhanden) wird geloggt und übersprungen statt die Ausführung abzubrechen — bewusst **kein** `ON CONFLICT DO NOTHING` in den (auch künftig von echten Use Cases genutzten) Repository-`Create`-Methoden, um dort die reale Konflikterkennung nicht aufzuweichen. Zweifach manuell verifiziert: `go run ./cmd/seed` gegen `db-test` erzeugt beim ersten Lauf alle sechs Datensätze, beim zweiten Lauf identische Aufrufe ohne Fehler mit „already exists, skipping" für alle sechs. `go build`/`go vet`/`go test -count=1 ./...` grün; `go test -count=1 -p 1 ./...` gegen `db-test` grün inkl. `TestResourceLifecycleE2E` mit neuem UoW-Seeding; SKIP-Zähler-Nachweis (`grep -c -- "--- SKIP"`) ergab 0 für `internal/adapters/postgres` und `internal/adapters/http`. **Bekannte Lücke:** `go test -race` konnte in dieser Umgebung nicht ausgeführt werden — es fehlt ein C-Compiler für cgo (`cl.exe`/`gcc`/`clang` alle nicht vorhanden); die vollständige Verifikationskette aus `agents.md` §11.3 ist damit nur ohne `-race` erbracht, das `-race`-Kettenglied steht auf einer Maschine mit installiertem C-Toolchain noch aus.
- [x] **Phase 2 (Auth-Kern), Punkte 5–7 umgesetzt und verifiziert** (2026-07-26): Migration `000007` erweitert `users` um `username` (Backfill von `id` für Bestandszeilen → `NOT NULL` → `CREATE UNIQUE INDEX uq_users_username`), `email` (nullable) und `version` (`integer NOT NULL DEFAULT 1`) — `created_at`/`updated_at` existierten bereits seit Migration `000001` und wurden bewusst **nicht** erneut hinzugefügt (Diskrepanz zur Aufgabenstellung erkannt, in der Migration dokumentiert statt stillschweigend einen doppelten Spaltenfehler zu riskieren). Migration `000008` legt `auth_identities` (`user_id` PK/FK, `password_hash NOT NULL` — bewusst vereinfacht auf lokal-passwortbasiert, kein Provider/OIDC in diesem Schritt, `D-5` bleibt offen), `sessions` (`id uuid`, `token_hash bytea UNIQUE` für den Access-Token, `active_role` per `CHECK` statt `roles(code)`-FK da die Rollen-Tabelle erst in Phase 3 entsteht) und `refresh_tokens` (`family_id`-basiertes Revoke statt `previous_token_id`-Kette — ein `UPDATE ... WHERE family_id = $1` statt rekursivem Walk; `successor_token_id IS NOT NULL` markiert „bereits verbraucht", kein separates `consumed_at`) inkl. Indizes auf `user_id`/`session_id`/`family_id` an. Alle neuen FKs nutzen `ON DELETE RESTRICT`, konsistent mit dem Rest des Schemas (User werden nie hart gelöscht). `domain.User` um `Username`, `Email *string`, `Version int64` erweitert; `postgres.userRepository.Create`/`GetByID` entsprechend angepasst. Neue Ports in `internal/application/ports/auth.go`: `AuthIdentityRepository`, `SessionRepository`, `RefreshTokenRepository`, `PasswordHasher`, `SecretGenerator`, `Clock` — bewusst noch nicht in `ports.Transaction` verdrahtet (keine Adapter/Use-Cases in diesem Schritt). `systemdesign.md` §5.2/§7.2/§7.3 auf den tatsächlich gelieferten (vereinfachten) Schema-Stand nachgeführt, inkl. Korrektur der veralteten `consumed_at`-Erwähnung in der SEC-08-Beschreibung. Die vier bestehenden Roh-SQL-`INSERT INTO users`-Stellen in `postgres_test.go`/`e2e_test.go` (`seedCoreRefs` u.a.) wurden um die jetzt Pflicht-Spalte `username` ergänzt, sonst hätte die neue `NOT NULL`-Constraint alle Integrationstests brechen lassen. **Verifiziert gegen `db-test`**: `go build`/`go vet`/`go test -count=1 ./...` grün; `go test -count=1 -p 1 ./...` gegen `db-test` grün (SKIP-Zähler 0 für `internal/adapters/postgres` und `internal/adapters/http`); `\d users`/`\d sessions`/`\d refresh_tokens`/`\d auth_identities` in `db-test` bestätigen das tatsächlich angewendete Schema; `cmd/seed` läuft nach den neuen Migrationen weiterhin unverändert fehlerfrei und idempotent (zwei Läufe geprüft). `-race` weiterhin nicht ausführbar (siehe Lücke oben, unverändert).

- [x] **Phase 2 abgeschlossen (2026-07-26)**: `ChangeOwnPassword` prüft und ersetzt Credentials transaktional, schreibt für Erfolg und Fehlversuch Audit-Events und widerruft andere Sitzungen. Login und Refresh sind durch eine begrenzte Drosselung nach IP bzw. Login-Konto geschützt; die Header-basierte IP-Ermittlung ist nur bei explizitem `TRUST_PROXY_HEADERS=true` aktiv. `go build ./...`, `go vet ./...` und `TEST_DATABASE_URL=postgres://dev:dev@localhost:5433/resource_test?sslmode=disable go test -count=1 -p 1 ./...` sind grün, PostgreSQL-Skip-Count `0`. Die vollständige Race-Suite ist ebenfalls über WSL2 gegen den Windows-gehosteten `db-test` grün: `TEST_DATABASE_URL="postgres://dev:dev@${WIN_IP}:5433/resource_test?sslmode=disable" go test -count=1 -p 1 -race ./...`.

- [x] **Phase-4-Blocker aus externem Auth/Session-Review behoben (2026-07-26)**: `RefreshSessionUseCase` wurde auf sichere Lock-Reihenfolge (`user_roles` → `sessions` → `refresh_tokens` → ggf. Familie) inkl. Re-Validierung nach Lock umgestellt; Access-Token-TTL-Bug in beiden Refresh-Pfaden behoben; `SwitchActiveRoleUseCase` race-fest gemacht (Role-Lock vor Session-Lock, Session-Validierung, Revoke vor Neuerstellung, Audit-ActorRole aus alter gesperrter Session); `LoginUseCase` prüft Legacy-Rolle gegen `user_roles` und behandelt fehlende Zuweisung identisch zu `!user.IsActive`; `sessionRepository.Revoke` prüft `RowsAffected`; `validateAuditMeta` vor allen relevanten `newAuditEvent`-Aufrufen ergänzt; veraltetes `requireRoles` inkl. dedizierter Tests entfernt. Neue Nachweistests: `TestLoginRefreshSuccessAndAccessTokenExpiresByAccessTTL_E2E`, `TestRefreshConcurrentConsumedPredecessorAndSuccessorNoDeadlock_SEC08`, `TestRefreshRoleRemovalConcurrentWithRefreshPreventsNewTokens_SEC14`, `TestLoginRevokedLegacyRoleUsesCredentialsInvalidPath`, `TestSwitchActiveRoleUserRolesErrorRollsBack`, `TestSwitchActiveRoleSessionSaveErrorRollsBack`, `TestSwitchActiveRoleAuditWriteErrorRollsBack`. Verifiziert mit `go build ./...`, `go vet ./...`, `TEST_DATABASE_URL=postgres://dev:dev@localhost:5433/resource_test?sslmode=disable go test -count=1 -p 1 ./...` sowie WSL2-Race-Lauf: `TEST_DATABASE_URL=postgres://dev:dev@10.255.255.254:5433/resource_test?sslmode=disable go test -count=1 -p 1 -race ./...`.

- [x] **Refresh-Token-Default-TTL auf Designvorgabe korrigiert (2026-07-26)**: `defaultRefreshTokenTTL` in `internal/application/usecases/login.go` von `7 * 24 * time.Hour` auf `30 * 24 * time.Hour` angehoben, damit der tatsächlich aktive Standard mit `systemdesign.md` §4 (Offline-Periode von bis zu 30 Tagen) übereinstimmt. Verifiziert mit `go test -count=1 ./internal/application/usecases -run 'TestLogin'`.

## ⏭️ Next Steps (in order)

### Phase 0 — Deployment-Grundlage
1. [x] Dockerfile (Multi-Stage, non-root User, `CGO_ENABLED=0`) und `compose.yaml` für Backend + PostgreSQL erstellt und verifiziert. Secrets nicht im Image; `AUTH_STATIC_TOKENS` nur zur Laufzeit im dev-Compose. Ein Container-`HEALTHCHECK` ist bewusst noch offen (Distroless ohne Shell/curl/wget; eigenes Binary wäre nötig, `/healthz` ist extern prüfbar).
   - [x] 1c umgesetzt: Integrations-/E2E-Tests wenden Embedded Up-Migrationen beim Teststart über `RunEmbeddedMigrations` in `testPool`/`e2eTestPool` auf `TEST_DATABASE_URL` an (ohne Refactoring auf `TestMain`).
   - [x] 1d (ehemals Punkt 15) umgesetzt: `AUTH_STATIC_TOKENS` ist nur bei `APP_ENV=dev` zulässig; bei `staging` oder `prod` bricht der Startup vor dem Start mit Fehler ab (SEC-26). Der Konfigurationstest `TestLoadConfigFromEnvSEC26RejectsStaticTokensOutsideDev` deckt beide Nicht-Dev-Umgebungen ab.

### Phase 1 — Audit-Fundament (Voraussetzung für alles Administrative) — ✅ abgeschlossen
2. [x] Migration `000006`: `audit_events.actor_role` CHECK um `'admin'` erweitert; `entity_type` um `user`, `role`, `user_role`, `resource_class`, `resource_class_membership`, `session`, `auth_identity` erweitert. Kein Foreign Key auf `roles(code)` (Audit muss von Stammdaten unabhängig bleiben, SEC-20-Begründung in `systemdesign.md` §8.1). Gegen die echte Testdatenbank verifiziert (siehe Punkt 3).
3. [x] Append-Only technisch erzwungen (SEC-20): `REVOKE UPDATE, DELETE ON audit_events` (vorbereitend, siehe `docs/deployment.md` Abschnitt 6) **plus** `BEFORE UPDATE OR DELETE FOR EACH ROW` sowie `BEFORE TRUNCATE FOR EACH STATEMENT`-Trigger mit `RAISE EXCEPTION ... USING ERRCODE = '42501'`. Integrationstests (`TestAuditWriterAndAppendOnlyTrigger`, `TestAuditEventsTruncateBlockedBySQLState`, `TestAuditEventsAcceptsAdminRoleAndNewEntityTypes` in `internal/adapters/postgres/postgres_test.go`) prüfen auf SQLSTATE `42501` statt Fehlertext. **Manuell gegen die echte Testdatenbank verifiziert** (zwei Läufe hintereinander gegen denselben `db-test`-Container, siehe Completed-Eintrag unten) — die grünen Skips zählen damit als vollwertiger Nachweis im Sinne von `agents.md`.
4. [x] Aktions-Taxonomie im Code als Konstanten festgeschrieben (`internal/domain/audit_action.go`, Typ `domain.AuditAction`): `ActionUserCreate`, `ActionUserUpdate`, `ActionUserDisable`, `ActionUserReactivate`, `ActionUserPasswordReset`, `ActionRoleAssign`, `ActionRoleRevoke`, `ActionSessionCreate`, `ActionSessionRevoke`, `ActionSessionReplayDetected`, `ActionAuthLoginFailed`, `ActionResourceClassCreate`, `ActionResourceClassUpdate`, `ActionResourceClassDeactivate`, `ActionResourceCreate`, `ActionResourceUpdate` (vollständige Liste aus `systemdesign.md` §8.1). `AuditEvent.Action` bleibt bewusst `string` (keine Änderung an bestehenden Use-Case-Aufrufstellen); die Konstanten sind für die noch zu bauenden Phase-2–4-Use-Cases vorgesehen, die diese Werte als Erste emittieren werden.

### Phase 2 — Auth-Kern (löst `AUTH_STATIC_TOKENS` ab) — ✅ abgeschlossen
- [x] Tech Debt vor Phase 2, Punkt 5 behoben: `seedDirectTransferPreconditions` und das Dev-Seeding laufen jetzt über UoW + Repositories statt handgeschriebenem SQL (siehe Completed-Eintrag oben). `domain.User` und die Ports `UserRepository`/`ResourceClassRepository` existieren bereits als Nebenprodukt — Punkt 7 unten ist entsprechend verkleinert.
5. [x] Migration `000007`: `users` um `username` (Backfill von `id` → `NOT NULL` → `UNIQUE`-Index), `email` (nullable) und `version` (`integer`, Default 1) erweitert. `created_at`/`updated_at` bereits seit Migration `000001` vorhanden, daher nicht erneut hinzugefügt (Diskrepanz zur ursprünglichen Aufgabenstellung erkannt und in der Migration dokumentiert). `users.role` unangetastet (Expand/Contract). `domain.User` und `postgres.userRepository` entsprechend erweitert.
6. [x] Migration `000008`: `auth_identities` (lokal-passwortbasiert, vereinfacht — kein Provider/OIDC in diesem Schritt, D-5 weiterhin offen), `sessions` (mit `token_hash` für den Access-Token) und `refresh_tokens` (mit `family_id`-basiertem Revoke statt `previous_token_id`-Kette) inkl. Indizes (`idx_sessions_user_id`, `idx_refresh_tokens_session_id`, `idx_refresh_tokens_family_id`) angelegt. `systemdesign.md` §5.2/§7.2/§7.3 entsprechend nachgeführt.
7. [x] Ports ergänzt (`internal/application/ports/auth.go`): `AuthIdentityRepository`, `SessionRepository`, `RefreshTokenRepository`, `PasswordHasher`, `SecretGenerator`, `Clock`. Bewusst noch **nicht** in `ports.Transaction`/`txAdapter` verdrahtet — es existieren noch keine Postgres-Adapter oder Use Cases dafür (folgt mit Punkt 8+).
8. [x] **Argon2id Adapter implementiert** (`internal/adapters/auth/argon2.go`, 2026-07-26): `Argon2Hasher` mit PHC-Kodierung (`$argon2id$v=19$m=<m>,t=<t>,p=<p>$<base64-salt>$<base64-hash>`), konfigurierbaren Parametern (Memory/Time/Parallelism/SaltLen), Constant-Time-Vergleich via `crypto/subtle.ConstantTimeCompare` und deterministischer `NeedsRehash`-Logik (erkennt veraltete Parameter und triggert transparentes Rehash bei nächstem Login — SEC-02). Umfassende Unit-Tests (`argon2_test.go`): Korrektheit des Hash-Formats, erfolgreiche/misslungene Verifikation, Rehash-Triggering bei Parameter-Änderungen, Rejection malformer PHC-Strings (leere/falsche Version/ungültiges Base64/fehlende Parameter), Constant-Time-Vergleich gegen 7 falsche Passwörter, Salt-Variation (5 Hashes des gleichen Passworts sind eindeutig), konfigurierbarer Parameter-Test (Memory/Time/Parallelism) — insgesamt 9 Top-Level-Tests (mehrere davon mit Subtest) + 1 Nebentest. **Gegen echte Standardbibliothek verifiziert**: `go build ./... && go vet ./... && go test -count=1 ./internal/adapters/auth/...` — alle Tests grün (39 Assertions über Argon2 + Tokens). `crypto/rand` wird korrekt für Salt-Generierung genutzt (kein `math/rand`).
9. [x] **Token Adapter implementiert** (`internal/adapters/auth/token.go`, 2026-07-26): `SecretGenerator` für Access- und Refresh-Tokens mit Format `rp_at_<uuid>.<base64url-secret>` / `rp_rt_<uuid>.<base64url-secret>`, ≥32 Byte Entropie aus `crypto/rand` (enforced minimum), `HashTokenSecret()` für SHA-256-Hashing zur DB-Speicherung (nur Hash, nie Plaintext), `ExtractSecretFromToken()` und `ExtractIDFromToken()` für sichere Token-Verarbeitung (kein String-Splitting). Umfassende Unit-Tests (`token_test.go`): Correctness Format-Validierung beider Token-Typen, Extraktion von ID und Secret, Kollisionsresistenz (10.000 Tokens sind eindeutig), SHA-256-Hash-Korrektheit (Determinismus + Vergleich mit `crypto/sha256` Reference), Entropy-Minimum-Durchsetzung, Token-Rekonstruktions-Symmetrie (100 Zyklen Extract+Reconstruct), Unabhängigkeit verschiedener Generator-Instanzen — insgesamt 15 Tests. **Gegen echte Standardbibliothek verifiziert**: `go build ./... && go vet ./... && go test -count=1 ./internal/adapters/auth/...` — alle Tests grün (51 Assertions über Tokens). `crypto/rand` wird ausschließlich für Secret-Generierung genutzt (kein `math/rand` — Grep-Nachweis: 0 Treffer im Code). `crypto/subtle` ist nicht nötig (Token-Secrets werden gehashed vor Speicherung, späterer Vergleich erfolgt mit gehashten Werten).
- [x] **Abhängigkeits-Updates**: `go.mod` um `github.com/google/uuid v1.6.0` (UUID v7 für Token-ID-Generation, BSD-3-Clause) und `golang.org/x/crypto v0.29.0` (für `golang.org/x/crypto/argon2`, BSD-3-Clause) ergänzt; `go mod tidy` erfolgreich; `go build ./...` ohne Fehler.
10. [x] `ChangeOwnPassword` (SEC-21): prüft das alte Passwort über den Argon2-Port, persistiert das neue Passwort innerhalb der UoW-Transaktion, auditiert Erfolg bzw. Fehlversuch und widerruft alle anderen aktiven Sitzungen. Use-Case-Tests decken Erfolg, Fehlversuch und Pflichtfelder ab; der Session-Widerruf ist zusätzlich gegen PostgreSQL getestet.
11. [x] Refresh-Rotation inkl. Replay-Erkennung: verbrauchter Token außerhalb `REFRESH_REPLAY_GRACE` ⇒ gesamte Token-Familie + Session widerrufen und auditieren (SEC-08). `RefreshSessionUseCase` sperrt Session/Refresh-Token, rotiert atomar, und schreibt `session.refresh` bzw. `session.replay_detected`. D-2 gibt innerhalb von `REFRESH_REPLAY_GRACE` den AES-256-GCM-verschlüsselten, bereits ausgegebenen Successor-Refresh-Token und einen neuen Access-Token zurück; es entsteht kein zweiter Successor. Migration `000009` ergänzt `refresh_tokens.encrypted_successor`; `ENCRYPTION_KEY` ist Base64-kodiert und muss nach Dekodierung 32 Byte enthalten. Unit-Tests `TestRefreshSessionGraceReturnsOriginalSuccessor_D2` und `TestRefreshSessionReplayOutsideGraceRevokesFamily_SEC08` belegen beide Sicherheitsfälle.
12. [x] `SessionAuthenticator` als neuer `ports.Authenticator`; Principal-Cache mit harter, über `PRINCIPAL_CACHE_TTL` konfigurierbarer TTL (Standard `30s`, SEC-11). Der Authenticator prüft den Access-Token-Hash gegen die aktive DB-Session und cachet Principals mutex-geschützt; Logout erhält die Session-ID ausschließlich aus diesem Principal.
13. [x] Drosselung für `login` und `refresh` (SEC-05): begrenzter, mutex-geschützter In-Memory-Fixed-Window-Limiter mit IP- und beim Login zusätzlich Account-Schlüssel; Überlastung liefert `429` mit `Retry-After`. Proxy-Header werden nur bei `TRUST_PROXY_HEADERS=true` ausgewertet. Unit-Tests beweisen beide IP-Modi und die Drosselungsantwort.
14. [ ] Timing-Gleichheit bei unbekanntem Benutzer: Dummy-Argon2id-Verifikation, einheitliche Fehlermeldung (SEC-03). Test, der die drei Fehlerfälle auf identische Response prüft.
### Phase 3 — Rollen & Berechtigungen
16. [x] Migration `000010`: `roles`, `permissions`, `role_permissions`, `user_roles` (inkl. `CHECK (assigned_by <> user_id)`) angelegt; Katalog aus `systemdesign.md` §6.2 geseedet; `system.is_assignable = false`.
17. [x] Migration `000011`: `user_roles` aus `users.role` backfilled.
18. [x] `PermissionResolver`-Port und PostgreSQL-Adapter sind umgesetzt; `SessionAuthenticator` löst Berechtigungen für die aktive Rolle serverseitig auf und cachet sie zusammen mit dem Principal (SEC-11/SEC-14, begrenzte konfigurierbare TTL mit Standard `30s`). Alle bestehenden geschützten Routen verwenden nun `requirePermissions` (AND), `requireAnyPermission` (OR) oder bewusst `requireAuthenticated` für Self-Service. Das abgestimmte Mapping ist umgesetzt: Admin besitzt keinen Zugriff auf `GET /api/v1/requests/{id}`; Dispatcher besitzt `allocation.return_request`.
   - [x] `POST /api/v1/requests` akzeptiert keine `technician_id` mehr. Die Request-Techniker-ID wird ausschließlich aus `Principal.UserID` abgeleitet; `decodeJSONBody` weist ein weiterhin gesendetes Feld als unbekannt mit `400 bad_request` zurück (SEC-01).
19. [x] Deny-by-Default-Test `TestAllProtectedRoutesDeclarePermissionsOrAreSelfService_SEC13`: die bei der geschützten Registrierung befüllte Route-Registry verlangt für Permission- und Any-Permission-Routen mindestens eine Berechtigung, akzeptiert ausschließlich deklarierte Self-Service-Routen ohne Berechtigung und schlägt bei unbekannter Art fehl (SEC-13).
20. [x] `active_role` in der Session; `SwitchActiveRole` erzeugt eine **neue** Session und widerruft die alte; Validierung von `active_role` gegen `user_roles` bei jedem Refresh (SEC-14). Die alte Refresh-Token-Familie wird beim ordentlichen Rollenwechsel nicht separat mutiert; ihr Refresh wird über die widerrufene Session abgewiesen. Bei nachträglichem Rollenentzug widerruft der Refresh-Ablauf die Sitzung und schreibt `session.revoke` mit `note="active_role_no_longer_held"`.
21. [x] `GET /api/v1/auth/me` liefert `user_id`, `active_role`, alle aktuell gehaltenen Rollen und ausschließlich die im authentifizierten Principal bereits aufgelösten effektiven Permissions der aktiven Rolle.

### Phase 4 — Admin-Endpunkte
22. [ ] Use Cases: `ListUsers`, `GetUser`, `CreateUser`, `UpdateUser`, `DisableUser`, `ReactivateUser`, `AssignRole`, `RevokeRole`, `RevokeUserSessions`, `ResetLocalPassword`, `ListUserAuditEvents`.
23. [ ] Invariante „letzter aktiver Admin" **race-frei** implementieren (SEC-18): `SELECT … FOR UPDATE` über die Admin-Zuweisungen bzw. transaktionsgebundener Advisory Lock. Integrationstest mit zwei parallelen Transaktionen, der beweist, dass nicht beide durchkommen.
24. [ ] Selbstzuweisung von Rollen im Use Case ablehnen (SEC-16); Negativtest gegen den DB-`CHECK` als zweite Verteidigungslinie.
25. [ ] Session-Widerruf bei Deaktivierung, Rollenänderung und Passwort-Reset (SEC-10); Test, dass ein vorher gültiger Access-Token innerhalb `PRINCIPAL_CACHE_TTL` ungültig wird.
26. [ ] `GET /api/v1/admin/audit-events` mit Permission `audit.read`, Filter nach Zeitraum, Actor und Entity; keine Schreibpfade.
27. [ ] E2E-Test `TestUserAdministrationE2E`: Admin legt Benutzer an, weist Rolle zu, Benutzer loggt sich ein, Admin deaktiviert ihn, Folgeaufruf ⇒ `401`; Negativfall: Admin versucht Direct Transfer ⇒ `403` (SEC-15).

### Phase 5 — Datenbank-Privilegien & .NET-Grenze
28. [ ] Drei getrennte PostgreSQL-Rollen: `app_backend` (DML, kein DDL), `app_migrator` (DDL), `refdata_tool` (nur Referenztabellen). `GRANT`/`REVOKE`-Skripte versionieren und im Deployment ausführen (SEC-24, SEC-25).
29. [ ] Integrationstest, der mit `refdata_tool`-Credentials beweist, dass Zugriffe auf `users`, `auth_identities`, `sessions`, `refresh_tokens` und `audit_events` scheitern.
30. [ ] `docs/deployment.md` um Rollen-, Secret- und TLS-Vorgaben ergänzen (SEC-27).

### Phase 6 — Optional / später
31. [ ] Aufbewahrungs-Job für Session- und Audit-Daten mit konfigurierbaren Fristen (Abschnitt 9).
32. [ ] OIDC-Adapter (Entra ID) mit Authorization Code Flow + PKCE, externer Systembrowser (RFC 8252), Mapping `provider_subject` → interner Benutzer. Abhängig von D-5.
33. [ ] Contract-Migration: `users.role` entfernen, sobald kein Codepfad mehr darauf liest (nicht im selben Release wie Phase 3).
34. [ ] Optional: `prev_hash`-Kette in `audit_events` für Manipulationsnachweis.

### Begleitend
35. [ ] `agents.md` um die neuen Architekturregeln ergänzen (Text siehe unten).

## 📌 Neue Regeln für `agents.md`
- Routen deklarieren **Permissions**, nie Rollen. Eine Route ohne Permission-Deklaration ist ein Fehler und muss `403` liefern (SEC-13).
- Actor-Identität und `actor_role` kommen ausschließlich aus dem authentifizierten Principal, niemals aus dem Request-Body (SEC-01).
- Kryptografie (Argon2id, `crypto/rand`, Hashing, Token-Kodierung) lebt ausschließlich in Adaptern. Die Domain kennt nur die Ports `PasswordHasher`, `SecretGenerator` und `Clock`.
- Jede sicherheitsrelevante Mutation läuft über die `UnitOfWork` und schreibt ihr `AuditEvent` in derselben Transaktion (SEC-21).
- Keine Zeit aus `time.Now()` in Domain oder Application — immer über den `Clock`-Port.
- Secrets, Token und Hashes erscheinen nie in Logs, URLs, Fehlermeldungen oder Audit-Payloads (SEC-06, SEC-23).
- Administrative Berechtigungen implizieren niemals operative (SEC-15).

## ⚠️ Known Issues / Tech Debt
- **StaticTokenAuthenticator** (`internal/adapters/auth`) ist eine Übergangslösung. Tokens stehen im Klartext in `AUTH_STATIC_TOKENS`. Ablösung in Phase 2; danach nur noch bei `APP_ENV=dev` zulässig und in Phase 3 vollständig entfernen.
- **Windows-Toolchain für `-race`**: WSL2 ist unter Windows der ausschließlich unterstützte Ausführungsweg für die vollständige Race-Suite. Die native Visual-Studio-/Windows-C-Toolchain darf dafür nicht eingerichtet, gewechselt oder diagnostiziert werden. `db-test` läuft auf dem Windows-Host mit der Portfreigabe auf allen Interfaces (`"5433:5432"`, nicht `127.0.0.1:5433:5432`); WSL2 führt die Tests über die Windows-Host-IP aus. Die genaue Anleitung steht in `README.md` unter „Race Tests from WSL2“.
- **Audit-Unveränderlichkeit**: seit Migration `000002` per Trigger technisch erzwungen (nicht nur Design-Zusage); seit `000006` zusätzlich mit stabilem SQLSTATE `42501` und `TRUNCATE`-Schutz, verifiziert gegen die echte Testdatenbank. Die in `000006` ergänzten `REVOKE`-Statements sind **vorbereitend, nicht wirksam**: Backend, Migrationen und .NET-Tool laufen weiterhin unter demselben (Eigentümer-)Konto, das von `REVOKE ... FROM PUBLIC` nicht betroffen ist. Echte Durchsetzung erst mit der Rollentrennung in Phase 5 (siehe `docs/deployment.md` Abschnitt 6).
- **`users`-Tabelle zu eng** (`id`, `role`, `display_name`, `is_active`): kein `username`, keine Mehrfachrollen, keine Versionierung.
- **Datenbank-Privilegien nicht getrennt**: Backend, Migrationen und das .NET-Tool nutzen bislang dasselbe Konto. Die dokumentierte Grenze des .NET-Tools ist damit reine Konvention und nicht durchgesetzt (SEC-25). Die Credentials eines kopierbaren Desktop-Clients sind als kompromittiert zu betrachten.
- **Offene Entscheidung D-3**: Techniker dürfen derzeit alle Requests lesen. Bewusst gewählt und auditiert, aber vor dem Feldeinsatz mobiler Clients auf `request.read.own` einzuschränken.
- **Kein Aufbewahrungskonzept** implementiert; Audit- und Session-Daten wachsen unbegrenzt (Datenschutz, Abschnitt 9).
- **IdempotencyStore ist als Port und PostgreSQL-Adapter implementiert, wird aber von keinem Use Case aufgerufen** (Grep-Nachweis: nur in `usecases_test.go`). `agents.md` §5 formuliert die Prüfung als verbindliche Regel, obwohl es keinen Aufrufer gibt — der Batch-Sync-Endpunkt fehlt noch. Bis dahin ist die Regel nicht durchsetzbar. Beim Bau des Sync-Endpunkts zuerst prüfen.

## 📝 Rules for the AI Agent
- **READ THIS FILE FIRST** at the start of every session or task.
- **UPDATE THIS FILE** immediately when a task from "In Progress" or "Next Steps" is finished.
- Move completed items to the "Completed" list and keep the "Current Focus" sharply aligned with the current immediate goal.
- Reference the relevant `SEC-xx` requirement from `systemdesign.md` §11 in commit messages and tests for every security-relevant change.
- No administrative endpoint ships before Phase 1 (audit extension) is complete.
- At the end of every phase, run the PostgreSQL integration tests against the real test database (`TEST_DATABASE_URL` → `resource_test`) — green skips are not sufficient once the environment is available.